/* If I use the triangle nav. 1) to get the number of lis in ul
document.GetElementById("nav-items").GetElementsByTag("li").length
2) to target the menu ":after or :before" 
element and change the style dynamically based on the number of 
<li>s in the <ul>. */
// var styleElem = document.head.appendChild(document.createElement("style"));
// styleElem.innerHTML = "#theDiv:before {background: green;}";